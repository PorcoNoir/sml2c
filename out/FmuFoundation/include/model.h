/* model.h — generated by sml2c for FMU 'Foundation'.
 * Do not edit by hand.
 *
 * Mirrors the variables in src/resources/modelDescription.xml.
 * Keep VR_* macros in sync with the valueReference attributes
 * in the model description (sml2c assigns them sequentially in
 * source-declaration order). */
#ifndef MODEL_H
#define MODEL_H

#include "fmi3PlatformTypes.h"

/* Value references (1-indexed, source-declaration order). */
#define VR_mass 1
#define VR_capacity 2
#define VR_label 3
#define VR_enabled 4

typedef struct {
    fmi3Float64  mass;
    fmi3Int64    capacity;
    fmi3String   label;
    fmi3Boolean  enabled;
} ModelData;

/* Set every field to its modelDescription start value.
 * Called from fmi3InstantiateCoSimulation. */
void model_setDefaults(ModelData* m);

#endif /* MODEL_H */
